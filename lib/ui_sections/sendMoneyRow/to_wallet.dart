import 'package:flutter/material.dart';

class ToWallet extends StatelessWidget {
  const ToWallet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      body: Column(
        children: [Text("To Wallet Screen")],
      ),
    ));
  }
}
