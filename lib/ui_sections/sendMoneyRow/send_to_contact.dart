import 'package:flutter/material.dart';

class ToContactSend extends StatelessWidget {
  const ToContactSend({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      body: Column(
        children: [Text("To Contact Send")],
      ),
    ));
  }
}
