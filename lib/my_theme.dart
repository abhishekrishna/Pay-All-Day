import 'package:flutter/material.dart';

class MyTheme {
  /*configurable colors stars*/
  static Color accent_color = Colors.indigo.shade900;
  static Color soft_accent_color = Colors.blue.shade200;
  static Color splash_screen_color =
      Colors.white; // if not sure , use the same color as accent color
  /*configurable colors ends*/
  static Color black_color = Colors.black;
  /*If you are not a developer, do not change the bottom colors*/
  static Color white = Color.fromRGBO(255, 255, 255, 1);
  static Color light_grey = Color.fromRGBO(0, 0, 0, 1);
  static Color dark_grey = Color.fromRGBO(0, 0, 0, 1);
  static Color medium_grey = Color.fromRGBO(0, 0, 0, 1);
  static Color medium_grey_50 = Color.fromRGBO(0, 0, 0, .5);
  static Color grey_153 = Color.fromRGBO(0, 0, 0, 1);
  static Color font_grey = Color.fromRGBO(0, 0, 0, 1);
  static Color textfield_grey = Color.fromRGBO(0, 0, 0, 1);
  static Color golden = Color.fromRGBO(248, 181, 91, 1);
  static Color shimmer_base = Colors.grey.shade200;
  static Color shimmer_highlighted = Colors.grey.shade300;

  //testing shimmer
  /*static Color shimmer_base = Colors.redAccent;
  static Color shimmer_highlighted = Colors.yellow;*/

}
